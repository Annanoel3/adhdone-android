name: Android Build (signed)

on:
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Prepare keystore at repo root
        shell: bash
        run: |
          set -e
          if [ -n "${{ secrets.KEYSTORE_BASE64 }}" ]; then
            echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > release.keystore
            echo "KEYSTORE_PASSWORD=${{ secrets.KEYSTORE_PASSWORD }}" >> $GITHUB_ENV
            echo "KEY_PASSWORD=${{ secrets.KEY_PASSWORD }}"         >> $GITHUB_ENV
            echo "KEY_ALIAS=${{ secrets.KEY_ALIAS }}"               >> $GITHUB_ENV
          else
            KS_PASS="password"
            KEY_PASS="password"
            ALIAS="release"
            keytool -genkeypair -v -keystore release.keystore \
              -storepass "$KS_PASS" -keypass "$KEY_PASS" \
              -keyalg RSA -keysize 2048 -validity 10000 \
              -alias "$ALIAS" \
              -dname "CN=ADHDone,O=ADHDone,L=NA,ST=NA,C=US"
            echo "KEYSTORE_PASSWORD=$KS_PASS" >> $GITHUB_ENV
            echo "KEY_PASSWORD=$KEY_PASS"     >> $GITHUB_ENV
            echo "KEY_ALIAS=$ALIAS"           >> $GITHUB_ENV
            base64 release.keystore > my-release.keystore.base64
          fi

      - name: Build signed release (flavor: normal)
        working-directory: android
        env:
          KEYSTORE_PASSWORD: ${{ env.KEYSTORE_PASSWORD }}
          KEY_PASSWORD: ${{ env.KEY_PASSWORD }}
          KEY_ALIAS: ${{ env.KEY_ALIAS }}
        run: |
          set -e
          chmod +x ./gradlew
          # Build the normalRelease variant explicitly since you have a "normal" flavor
          ./gradlew --no-daemon assembleNormalRelease

      - name: Print SHA-256 of signed APK
        working-directory: android
        run: |
          set -e
          BT="$ANDROID_HOME/build-tools/$(ls -1 "$ANDROID_HOME/build-tools" | sort -V | tail -n1)"
          APK="app/build/outputs/apk/normal/release/app-normal-release.apk"
          if [ ! -f "$APK" ]; then
            echo "Signed APK not found at $APK. Listing outputs:" 
            ls -R app/build/outputs/apk || true
            exit 1
          fi
          echo "----- SHA-256 digest (copy; remove colons for JSON) -----"
          "$BT/apksigner" verify --print-certs "$APK" | sed -n 's/.*SHA-256 digest: //p'

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ADHDone-build-output
          path: |
            android/app/build/outputs/apk/normal/release/app-normal-release.apk
            my-release.keystore.base64
          if-no-files-found: ignore
